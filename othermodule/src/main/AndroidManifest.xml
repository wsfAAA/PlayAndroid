<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="test.opendingding.com.othermodule">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.TestActivity"></activity>
        <activity android:name=".activity.MainActivity"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".interfaced.InterfacedActivity" />
        <activity android:name=".dialogandpopup.DialogActivity" />
        <activity android:name=".activity.RetrofitActivity" />
        <activity android:name=".picture.ScaleImageActivity" />
        <activity android:name=".picture.PicActivity" />
        <activity android:name=".picture.clippic.MainActivity2" />
        <activity android:name=".picture.clippic.ShowImageActivity" />
        <activity android:name=".QRcode.QRCodeActivity" />
        <activity android:name=".permission.PermissionsActivity" />
        <activity android:name=".QRcode.SecondActivity" />
        <activity android:name=".QRcode.ThreeActivity" />
        <activity android:name=".html.HTMLActivity" />
        <activity android:name=".designmodel.ModelActivity" />
    </application>

</manifest>